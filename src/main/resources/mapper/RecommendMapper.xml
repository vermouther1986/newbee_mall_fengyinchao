<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="ltd.newbee.mail.newbeemail.dao.RecommendMapper">
	<resultMap id="BaseResultMap"
		type="ltd.newbee.mail.newbeemail.entity.IndexConfig">
		<id column="config_id" jdbcType="BIGINT" property="configId" />
		<result column="config_name" jdbcType="VARCHAR"
			property="configName" />
		<result column="config_type" jdbcType="TINYINT"
			property="configType" />
		<result column="goods_id" jdbcType="BIGINT" property="goodsId" />
		<result column="redirect_url" jdbcType="VARCHAR"
			property="redirectUrl" />
		<result column="config_rank" jdbcType="INTEGER"
			property="configRank" />
		<result column="is_deleted" jdbcType="TINYINT"
			property="isDeleted" />
		<result column="create_time" jdbcType="TIMESTAMP"
			property="createTime" />
		<result column="create_user" jdbcType="INTEGER"
			property="createUser" />
		<result column="update_time" jdbcType="TIMESTAMP"
			property="updateTime" />
		<result column="update_user" jdbcType="INTEGER"
			property="updateUser" />
	</resultMap>


	<select id="findRecommendShopList" resultMap="BaseResultMap">

		select distinct info1.*
		from tb_newbee_mall_goods_info as info1
		join tb_newbee_mall_goods_info as info2
		on info2.goods_category_id = info1.goods_category_id
		join rescent_check_goods as chk
		on chk.goods_id = info2.goods_id
		join tb_newbee_mall_goods_category as cat2
		on info1.goods_category_id = cat2.category_id
		left join run_recommend_api_history as hi
		on cat2.category_id = hi.category_id
		and hi.user_id = #{userid}
		where hi.category_id is null and chk.user_id = #{userid}
		limit #{list}


	</select>




</mapper> 