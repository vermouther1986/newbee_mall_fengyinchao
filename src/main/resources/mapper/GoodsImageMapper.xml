<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
    namespace="ltd.newbee.mail.newbeemail.dao.GoodsImageMapper">

    

    <select id="getGoodsImage" resultType="ltd.newbee.mail.newbeemail.entity.GoodsImage">
     SELECT * FROM goods_image  where goods_id=#{goodsId} order by goods_rank
     
     
     
     
    </select>

</mapper>